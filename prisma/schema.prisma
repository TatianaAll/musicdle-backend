generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum RoleName {
  USER
  ADMIN
  COACH
}

model Role {
  id    Int      @id @default(autoincrement())
  name  RoleName @unique
  users User[]

  @@map("roles")
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  username String
  password String

  roles Role[]

  @@map("users")
}

enum GameMode {
  classic
  top50fr
}

model Song {
  id        Int    @id @default(autoincrement())
  idSpotify String @unique
  track     String
  artist    String
  album     String
  year      String
  duration  Int
  cover     String
  genre     String
  dailyDate DateTime?
  gameMode  GameMode?  

  @@map("songs")
}